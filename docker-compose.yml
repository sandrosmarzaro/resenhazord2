services:
  app:
    build: .
    volumes:
      - .:/app
    tty: true
    working_dir: /app
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "process.exit(process.pid > 0 ? 0 : 1)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s