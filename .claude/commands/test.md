Run the test suite for the project.

Run these commands in parallel:

1. Gateway (TypeScript): `cd gateway && bun run test:run`
2. Python: `pytest -v`

After both complete, summarize which passed and which failed. If any fail, show relevant errors.